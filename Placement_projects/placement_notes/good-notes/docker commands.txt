
)docker images
shows images on machine

)docker container ls
show running container (same as docker ps)

)docker ls -a
list all containers on you machine (running and not running)

)docker exec <name> make -C /usr/local/repos/cloudflow
makes given container

)docker container stop <name>
stop running container

)docker container prune
delete all non running con

sudo docker run -d -it -p <>:<>
-d (in the background)
-it (interactive with container shell)
-p (map port 5000 of local machine to port 5000 of container)

docker exec -it -u <user> <container> <command>



docker container run 
docker container run -d -it --name cloudflow_dev -v /usr/local/repos/cloudflow:/usr/local/repos/cloudflow icedev-cpp-go-python-grpc sleep infinity

